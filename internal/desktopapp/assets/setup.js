document.addEventListener("DOMContentLoaded",()=>{const overlay=document.getElementById("wait-overlay"),message=document.getElementById("wait-message");document.querySelectorAll("form[data-wait-message]").forEach(form=>form.addEventListener("submit",()=>{message.textContent=form.dataset.waitMessage||"Working…";overlay.hidden=false;document.body.classList.add("waiting");form.querySelectorAll("button").forEach(button=>button.disabled=true)}));window.addEventListener("pageshow",()=>{overlay.hidden=true;document.body.classList.remove("waiting")})});
